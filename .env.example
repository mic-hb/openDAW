# AutoMIDI backend URL. Only needed in dev to point at a non-default
# backend (e.g. a remote staging server). Default is http://localhost:8000.
# In production, the Vite dev proxy is replaced by a reverse proxy
# (nginx/CloudFront) that maps /api to the backend; no env needed.
AUTOMIDI_API_URL=http://localhost:8000
